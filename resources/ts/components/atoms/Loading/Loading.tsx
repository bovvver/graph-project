import React from "react";
import { faCircleNotch } from "@fortawesome/free-solid-svg-icons";
import { StyledIcon } from "./Loading.styles";

const Loading = () => {
    return <StyledIcon icon={faCircleNotch} />;
};

export default Loading;
