import styled from "styled-components";

export const StyledHeader = styled.h2`
    padding: 2em 0;
    font-size: 3rem;
    text-transform: lowercase;
    font-weight: normal;
    text-align: center;
`;
